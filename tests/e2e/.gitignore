# Playwright run artefacts: traces, HTML report, JSON report, RTL diagnostics.
# Regenerated by every run. The screenshots themselves are deliberately NOT here
# — they are committed under docs/screenshots/ because the launch docs cite them.
.output/
